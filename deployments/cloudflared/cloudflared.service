[Unit]
Description=cloudflared tunnel for Ollama
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/cloudflared tunnel run ollama-tunnel
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
